<h3 class="page-header">Recently Added</h3>
<div>
    <% books.forEach(book => { %>
        <a href="/books/<%= book.id %>"><img height="150" src="<%= book.coverImagePath %>" ></a>
    <% }) %>
</div>